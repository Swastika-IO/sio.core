@model Sio.Cms.Lib.ViewModels.SioPages.ReadMvcViewModel
@foreach (var nav in Model.Modules.OrderBy(n => n.Priority))
{
    if (nav != null)
    {
        <partial name="@nav.Module.TemplatePath" model="@nav.Module" />
    }}
